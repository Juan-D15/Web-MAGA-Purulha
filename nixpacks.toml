[phases.setup]
nixPkgs = ["python3", "postgresql_16.dev", "gcc", "pkg-config", "cairo.dev", "pango", "gdk-pixbuf", "gobject-introspection", "glib", "libffi", "python3Packages.pycairo"]

[phases.install]
cmds = [
  "export PKG_CONFIG_PATH=$(find /nix/store -name 'cairo.pc' -path '*/cairo*dev*' 2>/dev/null | head -1 | xargs dirname):$PKG_CONFIG_PATH",
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt"
]